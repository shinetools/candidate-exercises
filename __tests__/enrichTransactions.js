const enrichTransactions = require('../enrichTransactions');

describe('enrichTransactions', () => {
  it('should enrich the list of transactions with tag information', () => {
    // to do
  });
});
